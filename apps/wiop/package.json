{
	"name": "@wanderlust/wiop",
	"version": "2.0.0",
	"private": true,
	"description": "Wanderlust image optimization proxy server",
	"type": "module",
	"module": "index.ts",
	"scripts": {
		"build-types": "tsc --build",
		"check-types": "tsc --noEmit",
		"dev": "bun run --hot index.ts",
		"start": "bun run index.ts"
	},
	"dependencies": {
		"ipx": "^3.1.1",
		"typescript": "catalog:"
	},
	"devDependencies": {
		"@types/bun": "catalog:",
		"@wanderlust/config": "workspace:*"
	}
}
