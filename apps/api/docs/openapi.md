# OpenAPI

- oRPC with the `OpenAPIHandler` is used for generating OpenAPI docs.
- Default spec viewer is `Scalar`. To open it, go to `http://localhost:3000/api`.
- To get the spec, go to `http://localhost:3000/api/spec.json`
