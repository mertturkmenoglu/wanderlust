//  @ts-check

/** @type {import('prettier').Config} */
const config = {
  semi: true,
  singleQuote: true,
  trailingComma: 'all',
  singleAttributePerLine: true,
  arrowParens: 'always',
};

export default config;
